# 🗺️ Настройка карт в Nuxt 3 проекте

## ✅ Установлено

Все необходимые пакеты уже установлены:

```bash
✅ leaflet@1.9.4
✅ vue-leaflet@latest
✅ vue-yandex-maps@latest
```

---

## 📦 Созданные файлы

### Компоненты

**1. `app/components/DynamicMap.vue`**
- Универсальный компонент карты
- Автоматический выбор между Яндекс и OpenStreetMap
- Поддержка SSR (ClientOnly)
- Красивый UI с индикатором загрузки

**2. `app/components/EventLocationMap.vue`**
- Компонент для страниц событий
- Отображение информации о месте
- Информация о транспорте и рейсах
- Готов к использованию

### Плагины

**3. `app/plugins/yandex-maps.client.ts`**
- Инициализация Яндекс Карт
- Автоматическая регистрация в Nuxt
- Client-side only (не выполняется на сервере)

### Composables

**4. `app/composables/useMaps.ts`**
- `getMapConfig()` - получить конфигурацию карт
- `getMapProvider()` - получить провайдера
- `getGeoInfo()` - получить геоинформацию

---

## 🔧 Конфигурация

### `nuxt.config.ts`

Добавлено в `runtimeConfig`:

```typescript
runtimeConfig: {
  public: {
    apiBase: 'http://api.camp.test',
    yandexMapsApiKey: '593670a6-8e5e-4895-9fe5-dbd37dde463a'
  }
}
```

### Переменные окружения

Создайте `.env` в корне проекта:

```bash
# API Backend
NUXT_PUBLIC_API_BASE=http://api.camp.test

# Яндекс Карты
NUXT_PUBLIC_YANDEX_MAPS_API_KEY=593670a6-8e5e-4895-9fe5-dbd37dde463a
```

---

## 🎨 Использование

### Пример 1: Простая карта

```vue
<template>
  <div>
    <h2>Место проведения</h2>
    <DynamicMap
      :latitude="55.7558"
      :longitude="37.6173"
      :zoom="14"
      title="Красная площадь"
      address="Москва, Красная площадь"
    />
  </div>
</template>
```

### Пример 2: Карта события

```vue
<template>
  <div>
    <EventLocationMap :event="event" />
  </div>
</template>

<script setup lang="ts">
const event = {
  venue_name: 'Парк Горького',
  venue_address: 'Москва, ул. Крымский Вал, 9',
  venue_description: 'Центральный парк культуры и отдыха',
  venue_latitude: 55.7311,
  venue_longitude: 37.6014,
  airport_distance: '25 км от Внуково',
  recommended_flights: [
    'SU 1234 Москва - Санкт-Петербург',
  ],
};
</script>
```

### Пример 3: Использование на странице события

В `app/pages/event/[id].vue`:

```vue
<template>
  <div>
    <!-- Основная информация о событии -->
    <section class="event-header">
      <h1>{{ event.title }}</h1>
      <p>{{ event.description }}</p>
    </section>

    <!-- Карта места проведения -->
    <section class="event-location">
      <EventLocationMap :event="event" />
    </section>

    <!-- Остальная информация -->
  </div>
</template>

<script setup lang="ts">
const route = useRoute();
const { getEvent } = useEvents();

const event = await getEvent(route.params.id as string);
</script>
```

### Пример 4: Использование composable

```vue
<script setup lang="ts">
const { getMapConfig, getMapProvider, getGeoInfo } = useMaps();

// Получить конфигурацию карт
const mapConfig = await getMapConfig();
console.log('Provider:', mapConfig.provider); // 'yandex' или 'openstreetmap'

// Получить только провайдера
const provider = await getMapProvider();
console.log('Provider:', provider); // 'yandex' или 'openstreetmap'

// Получить геоинформацию
const geoInfo = await getGeoInfo();
console.log('Country:', geoInfo.country_code); // 'RU', 'US', и т.д.
</script>
```

---

## 🗺️ Как это работает

```
Пользователь заходит на сайт
         │
         ▼
API запрос: /api/v1/maps/config
         │
         ▼
Backend определяет IP → Страна
         │
         ▼
    СНГ/ШОС/БРИКС?
         │
    ┌────┴────┐
    │ Да      │ Нет
    ▼         ▼
Яндекс Карты  OpenStreetMap
(API ключ)    (бесплатно!)
```

### Регионы

**Яндекс Карты (24 страны):**
- 🇷🇺 СНГ: Россия, Беларусь, Казахстан, и др. (10 стран)
- 🇨🇳 ШОС: Китай, Индия, Пакистан, Иран (4 страны)
- 🇧🇷 БРИКС: Бразилия, ЮАР, Египет, и др. (10 стран)

**OpenStreetMap:**
- 🌍 Все остальные страны мира
- ✅ Бесплатно, без API ключа!

---

## 🎯 Props компонента DynamicMap

```typescript
interface Props {
  latitude: number;      // Широта (обязательно)
  longitude: number;     // Долгота (обязательно)
  zoom?: number;         // Зум карты (по умолчанию: 14)
  title?: string;        // Заголовок маркера
  address?: string;      // Адрес
  showInfo?: boolean;    // Показывать информацию (по умолчанию: true)
  height?: string;       // Высота карты (по умолчанию: '400px')
}
```

---

## 🚀 Запуск

### Development

```bash
npm run dev
```

Карты будут автоматически работать на `http://localhost:3000`

### Production

```bash
npm run build
npm run preview
```

---

## 🔒 Безопасность

### ✅ Что сделано правильно:

1. **API ключ в .env** - не в коде
2. **Client-side плагин** - не выполняется на сервере
3. **Fallback механизм** - если API недоступен
4. **ClientOnly обертки** - для SSR совместимости

### 🛡️ Рекомендации:

1. **Ограничьте домены в Яндекс Картах:**
   - `camp-events.ru`
   - `*.camp-events.ru`
   - `localhost` (для разработки)

2. **Настройте CORS на backend:**
   ```php
   // В Laravel
   'allowed_origins' => ['https://camp-events.ru'],
   ```

---

## 🧪 Тестирование

### Локальное тестирование

```bash
# Запустите dev сервер
npm run dev

# Откройте в браузере
http://localhost:3000
```

### Тестирование с разных IP

Используйте VPN или прокси для проверки разных регионов:

- **VPN в Россию** → должны загрузиться Яндекс Карты
- **VPN в США** → должны загрузиться OpenStreetMap
- **VPN в Китай** → должны загрузиться Яндекс Карты (ШОС)

---

## 📊 Типы данных

### Event Interface

```typescript
interface Event {
  id: number;
  title: string;
  description?: string;
  venue_name?: string;
  venue_address?: string;
  venue_description?: string;
  venue_latitude?: number;
  venue_longitude?: number;
  airport_distance?: string;
  recommended_flights?: string[];
}
```

### MapConfig Interface

```typescript
interface MapConfig {
  provider: 'yandex' | 'openstreetmap';
  country_code: string;
  country_name?: string;
  city?: string;
  api_key?: string | null;
}
```

### GeoInfo Interface

```typescript
interface GeoInfo {
  country_code: string;
  country_name: string;
  city: string;
  latitude: number;
  longitude: number;
  use_yandex_maps: boolean;
}
```

---

## 🐛 Troubleshooting

### Проблема: Карты не загружаются

**Решение:**
1. Проверьте консоль браузера (F12)
2. Убедитесь, что backend запущен
3. Проверьте `NUXT_PUBLIC_API_BASE` в `.env`

### Проблема: "Cannot read property 'map' of undefined"

**Решение:**
Убедитесь, что компонент обернут в `<ClientOnly>`:

```vue
<ClientOnly>
  <DynamicMap ... />
</ClientOnly>
```

### Проблема: Яндекс Карты не загружаются

**Решение:**
1. Проверьте API ключ в `.env`
2. Проверьте ограничения домена в личном кабинете Яндекс
3. Откройте консоль браузера для деталей ошибки

### Проблема: OpenStreetMap не загружается

**Решение:**
1. Проверьте интернет-соединение
2. Убедитесь, что Leaflet CSS импортирован
3. Проверьте, что контейнер имеет высоту

---

## 📈 Производительность

### SSR оптимизация

Компоненты карт загружаются только на клиенте:

```vue
<ClientOnly>
  <DynamicMap ... />
</ClientOnly>
```

Это предотвращает:
- Ошибки SSR
- Увеличение размера HTML
- Проблемы с window/document

### Lazy loading

Leaflet загружается динамически:

```typescript
const L = await import('leaflet');
await import('leaflet/dist/leaflet.css');
```

### Кеширование

Backend кеширует геолокацию на 24 часа, что снижает нагрузку на API.

---

## 📚 Дополнительные ресурсы

### Документация

- [Nuxt 3 Documentation](https://nuxt.com/)
- [Vue Yandex Maps](https://yandex-maps-unofficial.github.io/vue-yandex-maps/)
- [Leaflet Documentation](https://leafletjs.com/)
- [OpenStreetMap Wiki](https://wiki.openstreetmap.org/)

### Backend документация

- `MAPS_INTEGRATION_GUIDE.md` - общая архитектура
- `OPENSTREETMAP_INTEGRATION.md` - детали OpenStreetMap
- `SETUP_MAPS.md` - настройка backend

---

## ✅ Чек-лист готовности

- [x] Пакеты установлены (`leaflet`, `vue-leaflet`, `vue-yandex-maps`)
- [x] Компоненты созданы (`DynamicMap.vue`, `EventLocationMap.vue`)
- [x] Плагин настроен (`yandex-maps.client.ts`)
- [x] Composable создан (`useMaps.ts`)
- [x] Конфигурация обновлена (`nuxt.config.ts`)
- [ ] `.env` файл создан с переменными
- [ ] Backend API доступен
- [ ] Компоненты протестированы
- [ ] Готово к деплою

---

## 🎉 Готово!

Система динамических карт полностью настроена для Nuxt 3:

- ✅ **Яндекс Карты** для 24 стран
- ✅ **OpenStreetMap** для остальных
- ✅ **Автоматический выбор** по геолокации
- ✅ **SSR совместимость**
- ✅ **TypeScript поддержка**
- ✅ **Красивый UI**
- ✅ **Composables для удобства**

**Следующий шаг:** Создайте `.env` файл и начните использовать карты! 🗺️🚀

