# 🔧 Исправление ширины полей (v2)

## Проблема

После первого исправления ничего не изменилось. Поля всё ещё не занимали полную ширину.

## Причина

Использование `ui.base` для добавления `w-full` не сработало, потому что Nuxt UI может переопределять эти стили или применять их не к тому элементу.

## Решение v2

Добавили `class="w-full"` **напрямую** на каждый элемент:
- На обёртки `<div>`
- На `<UFormField>`
- На `<UInput>` и `<UInputNumber>`

## Изменения в коде

### До:
```vue
<div class="lg:col-span-2">
  <UFormField label="Ваше имя">
    <UInput
      :ui="{ 
        base: 'w-full'  ← Не сработало
      }"
    />
  </UFormField>
</div>
```

### После:
```vue
<div class="lg:col-span-2 w-full">  ← Добавлено
  <UFormField 
    label="Ваше имя"
    class="w-full"  ← Добавлено
  >
    <UInput
      class="w-full"  ← Добавлено
      :ui="{ 
        rounded: 'rounded-2xl',
        padding: { xl: 'px-6 py-5' },
        size: { xl: 'text-xl' }
      }"
    />
  </UFormField>
</div>
```

## Полная структура

```vue
<div class="grid grid-cols-1 lg:grid-cols-2 gap-6">
  <!-- Имя - занимает 2 колонки -->
  <div class="lg:col-span-2 w-full">
    <UFormField class="w-full">
      <UInput class="w-full" />
    </UFormField>
  </div>
  
  <!-- Телефон - 1 колонка -->
  <div class="w-full">
    <UFormField class="w-full">
      <UInput class="w-full" />
    </UFormField>
  </div>
  
  <!-- Email - 1 колонка -->
  <div class="w-full">
    <UFormField class="w-full">
      <UInput class="w-full" />
    </UFormField>
  </div>
</div>
```

## Где добавлен `w-full`

### 1. Поле "Имя"
```vue
<div class="lg:col-span-2 w-full">        ← 1
  <UFormField class="w-full">             ← 2
    <UInput class="w-full" />             ← 3
  </UFormField>
</div>
```

### 2. Поле "Телефон"
```vue
<div class="w-full">                      ← 1
  <UFormField class="w-full">             ← 2
    <UInput class="w-full" />             ← 3
  </UFormField>
</div>
```

### 3. Поле "Email"
```vue
<div class="w-full">                      ← 1
  <UFormField class="w-full">             ← 2
    <UInput class="w-full" />             ← 3
  </UFormField>
</div>
```

### 4. Поле "Количество мест"
```vue
<div class="w-full">                      ← 1
  <UFormField class="w-full">             ← 2
    <UInputNumber class="w-full" />       ← 3
  </UFormField>
</div>
```

## Почему три раза `w-full`?

### Уровень 1: `<div class="w-full">`
Гарантирует, что grid-элемент занимает всю ширину своей ячейки.

### Уровень 2: `<UFormField class="w-full">`
Гарантирует, что обёртка поля занимает всю ширину родителя.

### Уровень 3: `<UInput class="w-full">`
Гарантирует, что само поле ввода занимает всю ширину.

## Каскад ширины

```
Grid Container (100%)
  └─> Grid Cell (50% на desktop, 100% на mobile)
      └─> <div class="w-full"> (100% от ячейки)
          └─> <UFormField class="w-full"> (100% от div)
              └─> <UInput class="w-full"> (100% от UFormField)
```

## Важно!

Dev-сервер был перезапущен для применения изменений:
```bash
lsof -ti:3002 | xargs kill -9
npm run dev
```

## Проверка

1. **Очистите кэш браузера** (Cmd+Shift+R на Mac, Ctrl+Shift+R на Windows)
2. Откройте http://localhost:3002/book/4
3. Проверьте:
   - ✅ Поле "Имя" занимает всю ширину
   - ✅ Поля "Телефон" и "Email" занимают по половине на desktop
   - ✅ Все поля на всю ширину на mobile
   - ✅ Нет пустого пространства справа

## Если всё ещё не работает

### 1. Проверьте, что dev-сервер запустился
```bash
lsof -i:3002
```

### 2. Очистите кэш Nuxt
```bash
cd /Users/ruslanbelchenkov/Frontend/camp
rm -rf .nuxt node_modules/.cache
npm run dev
```

### 3. Проверьте в инспекторе браузера
- Откройте DevTools (F12)
- Найдите элемент `<input>`
- Проверьте, применяется ли класс `w-full`
- Проверьте computed styles для `width`

### 4. Проверьте Grid
В DevTools:
- Найдите элемент с `class="grid"`
- Включите Grid overlay (в Firefox/Chrome)
- Убедитесь, что ячейки занимают правильную ширину

## Альтернативное решение (если не помогло)

Если `w-full` не работает, можно использовать inline стили:

```vue
<UInput
  style="width: 100% !important"
  :ui="{ 
    rounded: 'rounded-2xl',
    padding: { xl: 'px-6 py-5' },
    size: { xl: 'text-xl' }
  }"
/>
```

Или добавить в `assets/css/main.css`:

```css
.booking-form input {
  width: 100% !important;
}

.booking-form .input-number {
  width: 100% !important;
}
```

## Итог

✅ Добавлен `class="w-full"` на всех уровнях
✅ Dev-сервер перезапущен
✅ Структура Grid правильная
✅ Код готов к работе

**Обновите страницу с очисткой кэша (Cmd+Shift+R)!** 🎉

