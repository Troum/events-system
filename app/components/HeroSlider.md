# HeroSlider Component

Красивый слайдер для hero-секции с автоматической прокруткой, навигацией и оверлеем контента.

## Использование

### Базовое использование

```vue
<HeroSlider
  :images="['image1.jpg', 'image2.jpg', 'image3.jpg']"
  title="Название события"
  subtitle="Подзаголовок"
  location="Валенсия, Испания"
  date-start="2025-06-15"
/>
```

### С кастомным контентом

```vue
<HeroSlider
  :images="event.hero_images"
  :title="event.title"
  :subtitle="event.subtitle"
  :location="event.location"
  :date-start="event.date_start"
>
  <template #back-button>
    <UButton to="/events" icon="i-heroicons-arrow-left">
      Назад
    </UButton>
  </template>

  <template #additional-info>
    <div class="mt-4">
      <UBadge>Осталось 5 мест</UBadge>
    </div>
  </template>
</HeroSlider>
```

## Props

| Prop | Type | Required | Default | Description |
|------|------|----------|---------|-------------|
| `images` | `string[]` | ✅ | - | Массив URL изображений для слайдера |
| `title` | `string` | ✅ | - | Заголовок |
| `subtitle` | `string` | ❌ | - | Подзаголовок |
| `location` | `string` | ❌ | - | Местоположение (отображается с иконкой) |
| `dateStart` | `string` | ❌ | - | Дата начала (форматируется автоматически) |

## Slots

### `back-button`
Слот для кнопки "Назад" или другой навигации в левом верхнем углу.

```vue
<template #back-button>
  <UButton to="/events">Назад к списку</UButton>
</template>
```

### `additional-info`
Слот для дополнительной информации под основным контентом.

```vue
<template #additional-info>
  <div class="mt-4 flex gap-2">
    <UBadge color="green">Активно</UBadge>
    <UBadge color="blue">Популярно</UBadge>
  </div>
</template>
```

## Особенности

### ✨ Автоматическая прокрутка
- Слайды автоматически переключаются каждые 5 секунд
- Пауза при наведении мыши
- Возобновление при уходе курсора

### 🎮 Навигация
- Кнопки "Влево/Вправо" для ручного переключения
- Точки-индикаторы внизу для быстрого перехода
- Счетчик слайдов в правом верхнем углу (1 / 3)

### 🎨 Анимация
- Плавная анимация переключения (0.6s)
- Эффект slide-fade
- Hover эффекты на кнопках

### 📱 Адаптивность
- Полностью адаптивный дизайн
- Работает на всех устройствах
- Touch-friendly кнопки

### ♿ Доступность
- ARIA-labels для кнопок навигации
- Семантическая разметка
- Keyboard navigation support

## Стилизация

Компонент использует:
- Tailwind CSS классы
- Backdrop blur эффекты
- Градиентные оверлеи
- Transition анимации

## Пример с реальными данными

```vue
<script setup>
const event = ref({
  hero_images: [
    '/storage/events/hero1.jpg',
    '/storage/events/hero2.jpg',
    '/storage/events/hero3.jpg'
  ],
  title: 'Padel Camp Valencia 2025',
  subtitle: 'Незабываемое приключение в Испании',
  location: 'Валенсия, Испания',
  date_start: '2025-06-15'
})
</script>

<template>
  <HeroSlider
    :images="event.hero_images"
    :title="event.title"
    :subtitle="event.subtitle"
    :location="event.location"
    :date-start="event.date_start"
  >
    <template #back-button>
      <UButton
        to="/events"
        variant="soft"
        icon="i-heroicons-arrow-left"
        class="text-white bg-primary-200/50"
      >
        Назад к списку
      </UButton>
    </template>
  </HeroSlider>
</template>
```

## Технические детали

- **Высота**: `60vh` (минимум 500px)
- **Интервал автопрокрутки**: 5000ms (5 секунд)
- **Анимация**: 600ms ease
- **Z-index**: Навигация на уровне 10

## Зависимости

- Vue 3 Composition API
- Nuxt UI (`UIcon`, `UContainer`)
- `useImageUrl` composable (для обработки путей изображений)

